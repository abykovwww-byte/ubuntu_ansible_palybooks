- requirement: "Party and Showroom create/update accept only rp or training, legacy stored novel aggregates are archived idempotently without conversion or history deletion, archived is terminal, and legacy list/read/Turn Trace/dataset projections remain readable"
  level: подключено
  store: "Pydantic request schemas, parties/showroom_scenarios status migration, endpoint guards, legacy read projections and SQLite history"
  assertion: absence
  expectation: retired_novel_is_readable_but_never_new_or_executable
  probe_command: null
  expected: null
  waiver: "PR #68 merge 0fb0ab0 was Ansible-applied with failed=0. Read-only live SQLite reported novel_non_archived=0 and exactly one archived Novel party; there were no stored Novel Showroom scenarios. The rebuilt production image passed the focused archived/read/terminal regressions. That legacy party had zero turns, so preservation of non-empty historical Turn Trace/dataset content remains covered by schema and container tests rather than a live row example."
- requirement: "Active Gateway runtime, both GUIs and current WorldPack manifests/prompts/setup/rules expose no novel execution path while historical decision evidence may retain an explicitly retired reference"
  level: подключено
  store: "Gateway runtime source, Light GUI, Showroom GUI, active WorldPack files and current documentation"
  assertion: absence
  expectation: active_surfaces_do_not_offer_or_execute_novel
  probe_command: null
  expected: null
  waiver: "Applied Light GUI and Showroom browser checks found no Novel marker in active HTML or visible text, Showroom exposed only rp/training scenario values, and both consoles had zero warning/error entries. The production-image focused create/update and terminal regressions passed; no authenticated form was submitted against live data."
- requirement: "NVIDIA is absent from active provider catalog, auth, endpoint routing, UI, configuration, Compose/Ansible and all fallback/retry targets, while stored NVIDIA profiles, party references and diagnostic rows remain readable and are never silently migrated"
  level: подключено
  store: "provider catalog/auth/runtime settings, model selectors, IaC templates and historical SQLite projections"
  assertion: absence
  expectation: nvidia_is_legacy_read_only_and_never_an_active_route
  probe_command: null
  expected: null
  waiver: "Applied runtime introspection exposed only local/openrouter service providers, no NVIDIA environment names, and NVIDIA supplied through LLM_PROVIDER normalized to the supported default. Read-only SQLite preserved 57 historical NVIDIA profiles and 26 referenced parties without reassignment, while Light GUI exposed only Gemini/OpenRouter provider options. No live turn was sent through a historical NVIDIA party; fail-closed execution is established by the rebuilt-container regression, not a destructive production canary."
- requirement: "Service roles route explicitly to local or OpenRouter without inheriting LLM_PROVIDER; an unavailable local service model degrades without provider switching and creates no new service_call_log row whose provider is nvidia"
  level: подключено
  store: "service-model selection/runtime, outbound request attempt and service_call_log"
  assertion: absence
  expectation: local_service_outage_never_routes_or_logs_nvidia
  probe_command: null
  expected: null
  waiver: "The rebuilt production Gateway image passed test_disabled_local_service_model_never_switches_provider: the forced unavailable-local attempt records local only and no NVIDIA row. Live settings selected local explicitly, the service catalog contained only local/openrouter, and read-only SQLite found zero NVIDIA service_call_log rows after apply. A real production local-runner outage was intentionally not induced, so this evidence establishes only подключено."
