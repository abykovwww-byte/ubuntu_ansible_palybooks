- requirement: "Revision 8 narrator prompt uses a 50-to-57-unit recent window whose start is quantized by eight plus every unit newer than minimum story-section coverage; RAW follows immutable narrator, world, compact PLAYER_CHARACTER and absolute rules and precedes volatile memory, lore, relationship pressure, author note and current action; the full legacy state, character, scene, episodic-memory and archive-retrieval layers are absent"
  level: каркас
  store: "recorded turns.prompt_json, turn metadata prompt_assembly, eligible raw turns and effective story-memory snapshot"
  assertion: prompt_presence
  expectation: revision8_history_first_prompt_has_no_coverage_gap
  probe_command: null
  expected: null
  waiver: "The first 60-turn production endurance proved the sliding RAW window and cache anchor but also showed that player identity disappeared after opening. Source now keeps only stable player name and description in ordinary and repair prompts; corrected post-apply narration remains pending."
- requirement: "Revision 8 records cached_prompt_tokens, prompt_tokens and a SHA-256 hash of immutable rules plus the anchored first 50 RAW units; the hash stays equal within an eight-turn bucket and changes when the actual RAW anchor moves, without adding provider cache calls, headers or settings"
  level: каркас
  store: "turn metadata_json, recorded turns.prompt_json and response_json usage.prompt_tokens_details"
  assertion: projection
  expectation: revision8_prompt_cache_prefix_is_quantized_and_observable
  probe_command: null
  expected: null
  waiver: "The first 60-turn production endurance observed the anchor move, cold cache and subsequent warm cache without extra provider calls. The player identity addition is now part of the same stable prefix; post-apply hash continuity remains pending."
- requirement: "Revision 8 creates no story memory through 50 eligible units, then one job processes one oldest-first batch of at most eight complete units in one five-section request, structurally validates each section, retries only invalid sections, keeps empty sections valid and uses per-section fresh, stale or failed status with safe minimum coverage"
  level: каркас
  store: "service_jobs, rp_story_memory_snapshots and service_call_log section rows"
  assertion: projection
  expectation: revision8_story_memory_sections_advance_independently
  probe_command: null
  expected: null
  waiver: "The first 60-turn production endurance executed two combined five-section OpenRouter updates with advancing minimum coverage. A forced deployed structural section failure remains pending."
- requirement: "Revision 8 story-memory calls use explicit provider openrouter and model deepseek/deepseek-v4-pro, complete-turn serialized messages no longer than 20000 characters, strict section JSON Schema without an artificial output max_tokens ceiling, at most two durable job attempts, section_key and update_id trace fields and no local or NVIDIA fallback"
  level: каркас
  store: "exact redacted service_call_log requests and durable service_jobs attempts"
  assertion: absence
  expectation: revision8_story_memory_never_routes_to_nvidia
  probe_command: null
  expected: null
  waiver: "Local payload and trace tests cover explicit provider/model, section_key=all versus exact retry keys, 20000-character complete-turn fitting, strict section JSON Schema without an artificial output cap and two-attempt scheduling. Production party party_c82153b0c2da recorded combined OpenRouter DeepSeek section calls and no NVIDIA route; a deployed structural section retry remains pending."
- requirement: "Revision 8 relationship prompt scope uses current input plus three complete previous eligible RAW units and excludes scene_state, seed location and active-thread presence signals while rendering only canonical human-readable names; this candidate scan never authorizes moving or introducing an absent NPC"
  level: каркас
  store: "recorded prompt_json relationship blocks, raw turns and existing relationship projections"
  assertion: absence
  expectation: revision8_relationship_scope_uses_recent_raw_scan_only
  probe_command: null
  expected: null
  waiver: "The first 60-turn production endurance proved alias rendering but exposed a due-event instruction teleporting a remote NPC into the scene. Source now requires actual presence or an established recent channel and otherwise defers the still-active event; corrected post-apply scene evidence is pending."
