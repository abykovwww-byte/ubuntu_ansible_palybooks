- requirement: "Revision-8 RP WorldPacks may declare reviewed rp-gateway.worldpack-lore-cards.v1 files; stable keys and non-empty keywords are validated, NPC cards cover canonical relationship aliases with always_on false, merchant-sviatoslav has at least 15 cards, and new parties copy them without a runtime model call or WorldPack mutation"
  level: каркас
  store: "WorldPack manifest and lore-cards JSON, party-scoped lore_cards rows, service_call_log"
  assertion: projection
  expectation: authored_lore_cards_are_reviewed_and_imported_without_runtime_generation
  probe_command: null
  expected: null
  waiver: "Repository validation and focused local Gateway tests cover schema, duplicate keys, non-empty keywords, NPC alias coverage, the 16-card merchant pack, party-scoped import and zero service calls during creation. Source merge, Ansible apply and live-party evidence are not claimed."
- requirement: "Revision-8 Lore Card and relationship selection share current player input plus three previous complete eligible RAW units and optional Outcome.target; Lore Cards match only whole title or keyword triggers and never their hidden content, while seed location, active threads and scene_state are excluded"
  level: каркас
  store: "eligible raw turns, recorded prompt_json and turn metadata prompt_assembly.lore_card_ids"
  assertion: absence
  expectation: rev8_lore_scan_is_recent_whole_and_never_self_activates_hidden_content
  probe_command: null
  expected: null
  waiver: "Deterministic local tests cover an expired fourth prior mention, whole Russian alias matching, substring rejection and hidden-content non-activation. The long-party causal check is deferred until the full RP-core implementation."
- requirement: "Revision-8 PARTY_LORE_CARDS serializes only complete cards inside 4000 characters and is removed as one optional block before protected RAW when the provider input budget overflows"
  level: каркас
  store: "recorded prompt_json, prompt_assembly omitted_blocks and lore_card_ids"
  assertion: absence
  expectation: lore_cards_are_whole_bounded_and_first_to_leave_on_hard_overflow
  probe_command: null
  expected: null
  waiver: "Local prompt-budget tests cover an oversized skipped card, a later complete card, the 4000-character bound and whole-block eviction before complete covered RAW units. No deployed overflow canary has been run."
- requirement: "An explicit revision-8 player action drafts one Lore Card from selected complete turns with one stack-key OpenRouter deepseek/deepseek-v4-pro request of at most 8000 input characters and 400 output tokens, with no local NVIDIA BYOK retry or fallback, and persists no card before player confirmation"
  level: каркас
  store: "draft API response, exact redacted service_call_log request/response and party lore_cards rows"
  assertion: projection
  expectation: lore_card_draft_is_bounded_logged_and_confirmation_gated
  probe_command: null
  expected: null
  waiver: "A local MockTransport integration test proves exact role/provider/model, stack-managed Authorization, max_tokens=400, prompt length, non-empty raw response, no pre-confirm row and source IDs after confirm. No real OpenRouter draft has been requested."
- requirement: "Turn history exposes raised Lore Cards in exactly the order of final post-budget prompt_assembly.lore_card_ids and Light GUI renders those chips below the matching narrator response together with the explicit draft button"
  level: каркас
  store: "turn metadata_json, history API activated_lore_cards projection and Light GUI chat DOM"
  assertion: projection
  expectation: player_sees_only_cards_that_reached_that_narrator_prompt
  probe_command: null
  expected: null
  waiver: "Focused Python and dependency-free JavaScript tests cover metadata IDs, history titles, exact ordering and draft UI markers. Browser and deployed-party verification remain pending."
