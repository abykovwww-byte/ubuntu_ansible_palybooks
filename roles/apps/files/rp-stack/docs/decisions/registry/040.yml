- requirement: "An RP WorldPack explicitly opts into a closed rp-gateway.rp-supervisor.v1 contract with fixed 50-turn window, eight-turn cadence, six canonical rule IDs and separate observe or enforce shapes"
  level: каркас
  store: "WorldPack manifest and rp-supervisor.json"
  assertion: projection
  expectation: rp_supervisor_contract_is_closed_pack_authored_and_observe_first
  probe_command: null
  expected: null
  waiver: "Source validation and focused tests cover the contract, and day-watch-moscow-v2 declares observe mode. No applied runtime or real 56-turn retrospective exists yet."
- requirement: "Every due retrospective freezes exactly the last 50 canonical playable units at eligible counts 56 then every eight, never includes story memory or truncates input, and records unchecked without a provider call when the selected global service route cannot fit the full window"
  level: каркас
  store: "turns, service_jobs and rp_supervisor_evaluations"
  assertion: projection
  expectation: rp_supervisor_window_is_exact_due_idempotent_and_fail_open
  probe_command: null
  expected: null
  waiver: "Focused persistence tests cover cadence, exact boundaries and capacity failure. No applied runtime job or service-provider response has been observed."
- requirement: "Observe mode never changes narrator prompts, while enforce mode may select at most two evidence-backed WorldPack advisories, suppresses turn-return sentinel disagreement and stops the same directional reassertion after three consecutive evaluations"
  level: каркас
  store: "rp_supervisor_evaluations and recorded narrator prompt_json"
  assertion: prompt_presence
  expectation: rp_supervisor_advisories_are_low_authority_bounded_and_self_expiring
  probe_command: null
  expected: null
  waiver: "Focused tests cover observe absence, two-advisory selection, sentinel isolation, K=3 and prompt order. The activated pack remains observe, so no live advisory chain is claimed."
- requirement: "Supervisor persistence contains typed results and metadata only, omits raw prompt and response traces, expires after 30 days, invalidates windows touched by rollback and stays isolated across party lifecycle"
  level: каркас
  store: "rp_supervisor_evaluations, service_call_log, turns and party deletion lifecycle"
  assertion: absence
  expectation: rp_supervisor_private_windows_are_not_duplicated_into_diagnostics
  probe_command: null
  expected: null
  waiver: "Focused tests prove trace=false, typed storage and rollback invalidation; source owns expiry and party deletion ordering. Live SQLite privacy and TTL evidence remain pending after apply."
- requirement: "The owner-scoped party supervisor API and existing Light GUI memory tools distinguish disabled, observe, checked, unchecked and error states and show progress plus the selected global service model without adding a supervisor model selector"
  level: каркас
  store: "GET party supervisor response and Light GUI memory tools DOM"
  assertion: projection
  expectation: rp_supervisor_status_is_visible_without_new_routing_controls
  probe_command: null
  expected: null
  waiver: "Dependency-free JavaScript and Gateway tests cover source behavior. Authenticated deployed browser evidence remains pending until Ansible apply."
