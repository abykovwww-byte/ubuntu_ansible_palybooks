- requirement: "Revision-9 ordinary input is classified by one bounded local gm_intent request, explicit GM bypass is always available, and uncertain or unavailable classification commits nothing and returns the visible Master or Scene choice"
  level: каркас
  store: "service_call_log, party state version, turns and Light GUI route panel"
  assertion: absence
  expectation: uncertain_gm_intent_never_guesses_or_mutates_gameplay
  probe_command: null
  expected: null
  waiver: "Focused API and dependency-free UI tests cover the read-only uncertain response, explicit channel payload and visible route choice. No real local-runner outage or deployed browser flow has been exercised."
- requirement: "A revision-9 GM correction is limited to replacement or retraction of one existing memory fact, recent RAW assertion or absolute rule, uses a bounded local draft, and persists only after explicit idempotent confirmation without a narrator call or party-turn scene-time advance"
  level: каркас
  store: "turn_requests, state_versions, excluded gm_correction turns, audit_events and service_call_log"
  assertion: projection
  expectation: confirmed_gm_diff_is_atomic_idempotent_and_out_of_scene
  probe_command: null
  expected: null
  waiver: "Local endpoint tests cover reject, exact draft target revalidation, duplicate confirm, state version plus one, unchanged party turn and scene, and one excluded typed history row. Source is not activated or deployed."
- requirement: "At most 20 latest effective player-correction slots remain in a protected prompt overlay above RAW and story memory, with same-slot replacement allowed and no old correction resurfacing after the latest slot state is absorbed"
  level: каркас
  store: "turns.metadata_json player-correction artifacts, History API and recorded prompt_json"
  assertion: projection
  expectation: correction_overlay_is_bounded_latest_slot_authority
  probe_command: null
  expected: null
  waiver: "Focused tests cover pre-model limit rejection, same-slot allowance, latest terminal slot behavior and prompt order relative to memory, cards, relationship pressure and current action. No deployed hard-overflow prompt has been inspected."
- requirement: "A confirmed memory or RAW correction triggers exactly one affected OpenRouter story-memory section even before turn 50, becomes absorbed only after an exact user-authority fact or tombstone and target coverage persist, and leaves the other four coverages unchanged"
  level: каркас
  store: "service_jobs, service_call_log, rp_story_memory_snapshots and player-correction artifact status"
  assertion: projection
  expectation: correction_valve_absorbs_only_after_user_fact_and_section_coverage
  probe_command: null
  expected: null
  waiver: "The turn-12 deterministic test covers one section call, four zero coverages, authority user, GM-turn provenance and terminal absorption. A real OpenRouter call and long-party causal run are deferred."
- requirement: "Revision-9 GM intent, GM patch draft and relationship extraction route only to local Gemma with no NVIDIA fallback, relationship jobs become stale after five failures, while sectioned story memory remains explicit OpenRouter DeepSeek with two durable attempts"
  level: каркас
  store: "runtime settings, service_jobs and service_call_log"
  assertion: absence
  expectation: rev9_service_roles_never_inherit_nvidia_or_party_provider
  probe_command: null
  expected: null
  waiver: "Source and local tests prove explicit provider settings and terminal retry states. No production local outage, real OpenRouter correction or post-apply service log has been collected."
