- requirement: "Revision-9 ordinary input is classified by one bounded local gm_intent request, explicit GM bypass is always available, and uncertain or unavailable classification commits nothing and returns the visible Master or Scene choice"
  level: каркас
  store: "service_call_log, party state version, turns and Light GUI route panel"
  assertion: absence
  expectation: uncertain_gm_intent_never_guesses_or_mutates_gameplay
  probe_command: null
  expected: null
  waiver: "Focused API and dependency-free UI tests cover the read-only uncertain response, explicit channel payload and visible route choice. No real local-runner outage or deployed browser flow has been exercised."
- requirement: "A revision-9 GM correction is limited to replacement or retraction of one existing memory fact, recent RAW assertion or absolute rule, uses a bounded local draft, and persists only after explicit idempotent confirmation without a narrator call or party-turn scene-time advance"
  level: каркас
  store: "turn_requests, state_versions, excluded gm_correction turns, audit_events and service_call_log"
  assertion: projection
  expectation: confirmed_gm_diff_is_atomic_idempotent_and_out_of_scene
  probe_command: null
  expected: null
  waiver: "Local endpoint tests cover reject, exact draft target revalidation, duplicate confirm, state version plus one, unchanged party turn and scene, and one excluded typed history row. Production party party_c82153b0c2da confirmed one correction out of scene; corrected browser and post-apply closure evidence remains pending."
- requirement: "At most 20 latest effective player-correction slots remain in a protected prompt overlay above RAW and story memory, including compact narrator repair, with same-slot replacement allowed, older in-flight same-slot results ignored and no old correction resurfacing after the latest slot state is absorbed"
  level: каркас
  store: "turns.metadata_json player-correction artifacts, History API and recorded prompt_json"
  assertion: projection
  expectation: correction_overlay_is_bounded_latest_slot_authority
  probe_command: null
  expected: null
  waiver: "Focused tests cover pre-model limit rejection, full and repair prompt authority, same-slot replacement during an in-flight section call and latest terminal slot behavior. Corrected deployed repair and concurrent confirmation evidence is pending."
- requirement: "A confirmed memory or RAW correction triggers exactly one affected OpenRouter story-memory section even before turn 50, becomes absorbed only after an exact user-authority fact or tombstone and target coverage persist, and leaves the other four coverages unchanged"
  level: каркас
  store: "service_jobs, service_call_log, rp_story_memory_snapshots and player-correction artifact status"
  assertion: projection
  expectation: correction_valve_absorbs_only_after_user_fact_and_section_coverage
  probe_command: null
  expected: null
  waiver: "Production party party_c82153b0c2da exercised one real OpenRouter situation-section call before turn 50 and exposed a same-fact-id collision. Source now promotes the replacement in place, recovers a snapshot persisted before the artifact status flip without a second model call, and makes an already absorbed retry a no-op; corrected post-apply live absorption remains pending."
- requirement: "Revision-9 GM intent, GM patch draft and relationship extraction route only to local Gemma with no NVIDIA fallback; invalid relationship model output is audited and retried until the job becomes stale after five failures while valid events=[] succeeds once; sectioned story memory remains explicit OpenRouter DeepSeek with two durable attempts"
  level: каркас
  store: "runtime settings, service_jobs and service_call_log"
  assertion: absence
  expectation: rev9_service_roles_never_inherit_nvidia_or_party_provider
  probe_command: null
  expected: null
  waiver: "The first 60-turn production endurance showed 18 invalid relationship outputs incorrectly completing as success and zero new extracted causes. Source now routes each invalid output through the existing five-attempt stale policy while preserving valid empty success; corrected post-apply service evidence is pending."
