---
- name: Configure file-only AuditD forwarding in the isolated guest
  hosts: ngfw_audit_source
  become: true
  gather_facts: true
  serial: 1
  any_errors_fatal: true
  roles:
    - ngfw_audit_forwarder
