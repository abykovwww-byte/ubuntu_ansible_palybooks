interface:
  display_name: "RP World And Scenario Builder"
  short_description: "Author Decision 043 RP source"
  default_prompt: "Use $rp-world-pack-builder to update the validated Decision 043 World/Scenario source for day-watch-moscow-v2. Treat the production loader/schema as executable truth, route deterministic training to $training-world-pack-builder, and do not claim the new format is deployed or visible."
policy:
  allow_implicit_invocation: true
